# define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_ENV=test
APP_DEBUG=1
APP_SECRET='$ecretf0rt3st'
SYMFONY_DEPRECATIONS_HELPER=999999
# DATABASE_URL not overridden here: doctrine.yaml's dbname_suffix '_test' transforms
# the base 'larpm' (from .env) into 'larpm_test' for the test environment automatically.
