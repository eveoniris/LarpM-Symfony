{% extends "base.html.twig" %}

{% block title %}Objet{% endblock title %}

{% block body %}

	<ol class="breadcrumb">
		<li><a href="{{ path('homepage') }}">Accueil</a></li>
		<li><a href="{{ path('stockObjet.index') }}">Objets</a></li>
		<li><a href="{{ path('stockObjet.detail', {'objet': objet.id}) }}">{{ objet.nom }}</a></li>
		<li class="active">Suppression</li>
	</ol>
	
	
	<div class="well bs-component">
		{{ form_start(form, {"attr": {"action": path('stockObjet.delete', {'objet': objet.id}) }}) }}
			<fieldset>
				<legend>Supression d'un objet</legend>
					{{ objet.nom }}
					{{ objet.code }}
					{{ objet.description }}
					
					{{ form(form) }}
			</fieldset>
		{{ form_end(form) }}
	</div>
				
{% endblock body %}
