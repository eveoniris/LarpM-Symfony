{% extends "base.html.twig" %}

{% block title %}Groupes secondaires{% endblock title %}

{% block body %}

    {% include '_partials/breadcrumb.twig' with {'items' :
        [
            {'name' : 'Liste des groupes transverses', 'route': path('groupeSecondaire.list') },
            {'name' : "Détail de #{groupeSecondaire.label}", 'route': path('groupeSecondaire.detail', {'groupeSecondaire':groupeSecondaire.id}) },
            {'name' : "Mise en attente d'un postulant " ~ postulant.personnage.publicName },
        ]
    } %}

    <div class="well well-sm bs-component">

        <div class="header"><h5>Mise en attente d'un postulant</h5></div>

        <div class="list-group">
            <div class="list-group-item">
                <div class="list-group-item-heading">
                    <strong>{{ postulant.personnage.publicName }}</strong>
                </div>
                <div class="list-group-item-text">
                    {{ postulant.explanation|markdown_to_html }}
                </div>
            </div>

            <div class="list-group-item">
                <div class="list-group-item-heading">
                    <strong>Le message suivant sera envoyé au postulant :</strong>
                </div>
                <div class="list-group-item-text">
                    "Vous êtes accepté en tant que postulant, vous avez rendez-vous sur le GN pour que nous en parlions
                    en
                    jeu. Vous recevrez un rendez vous avant le GN."
                </div>
            </div>
        </div>

        <div class="mt-2">
            {{ form(form) }}
        </div>
    </div>

{% endblock body %}
