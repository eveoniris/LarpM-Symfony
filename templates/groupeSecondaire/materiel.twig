{% extends "base.html.twig" %}

{% block title %}Groupe transverse{% endblock title %}

{% block body %}

    <ol class="breadcrumb">
        <li><a href="{{ path('homepage') }}">Accueil</a></li>
        <li><a href="{{ path("groupeSecondaire.list") }}">Liste des groupes transverses</a></li>
        <li class="active">Modification du matériel d'un groupe transverse</li>
    </ol>

    <div class="well bs-component">
        {{ form_start(form, {"attr": {"action": path('groupeSecondaire.materiel.update',{'groupeSecondaire':groupeSecondaire.id}) }}) }}
        <fieldset>
            <legend>Matériel</legend>

            {{ form(form) }}
        </fieldset>
        {{ form_end(form) }}
    </div>

{% endblock body %}
