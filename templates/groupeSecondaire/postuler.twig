{% extends "base.html.twig" %}

{% block title %}Postuler à un groupe secondaire{% endblock title %}

{% block body %}

	<ol class="breadcrumb">
		<li>Navigation :</li>
		<li><a href="{{ path('homepage')  }}">Accueil</a></li>
		<li><a href="{{ path('gn.detail', {'gn' : participant.gn.id}) }}">{{ participant.gn.label }}</a></li>
		<li><a href="{{ path('gn.personnage', {'gn': participant.gn.id}) }}">{{ participant.personnage.nom }}</a></li>
		<li><a href="{{ path('participant.groupeSecondaire.admin.list', {'participant' : participant.id})  }}">Groupes secondaires</a></li>
		<li class="active">Postuler</li>
	</ol>

	<div class="well well-sm">
		{{ form_start(form, {"attr": {"action": path('participant.groupeSecondaire.postuler',{'participant' : participant.id, 'groupeSecondaire': groupeSecondaire.id}) }}) }}
			<fieldset>
				<legend>Postuler à {{ groupeSecondaire.label}}</legend>
				<h5>Remarque</h5>
				<p>Seul votre surnom sera transmis au responsable du groupe secondaire, si vous en possédez un, sinon ce sera votre nom.<p>
				<p>Dans le cas de groupes religieux, la liste de vos religions (ainsi que vos niveaux de Ferveur associés) sera transmis et visible par responsable du groupe secondaire.</p>
				
				
				{{ form(form) }}
			</fieldset>
		{{ form_end(form) }}
	</div>

{% endblock body %}