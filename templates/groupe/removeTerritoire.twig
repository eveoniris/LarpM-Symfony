{% extends "base.html.twig" %}

{% block title %}Groupes{% endblock title %}

{% block body %}

	<ol class="breadcrumb">
		<li><a href="{{ path('homepage') }}">Accueil</a></li>
		<li><a href="{{ path("groupe.list") }}">Liste des groupes</a></li>
		<li><a href="{{ path("groupe.detail", {'groupe': groupe.id}) }}">{{ groupe.nom }}</a></li>
		<li class="active">Retirer un territoire d'un  groupe</li>
	</ol>
	
	<div class="well well-sm">
		{{ form_start(form, {"attr": {"action": path('groupe.territoire.remove',{'groupe': groupe.id, 'territoire': territoire.id}) }}" method="POST">
			<div class="form-group">
				<p>Attention ! le groupe {{ groupe.nom }} va perdre le contrôle du territoire {{ territoire.nom }}</p>
			</div>
			
			{{ form(form) }}
			
		{{ form_end(form) }}
	</div>
{% endblock %}
