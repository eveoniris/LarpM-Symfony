{% extends "base.html.twig" %}

{% block title %}Groupes{% endblock title %}

{% block body %}

    <ol class="breadcrumb">
        <li><a href="{{ path('homepage') }}">Accueil</a></li>
        <li><a href="{{ path("groupe.admin.list") }}">Liste des groupes</a></li>
        <li><a href="{{ path("groupe.detail", {'groupe': groupe.id}) }}">{{ groupe.nom }}</a></li>
        <li class="active">Ajouter un participant</li>
    </ol>

    {{ include("_partials/form.twig",{
        'title': 'Ajout d\'un participant',
        'data': {'content': 'Ce formulaire ne liste que les utilisateurs n\'étant pas inscrit dans un groupe'}
        'form' : form}) }}

{% endblock %}
