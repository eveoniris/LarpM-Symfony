{% extends "base.html.twig" %}

{% block title %}Demander une alliance{% endblock title %}

{% block body %}

	<ol class="breadcrumb">
		<li><a href="{{ path('homepage') }}">Page d'accueil</a></li>
		<li><a href="{{ path('participant.index', {'participant' : participant.id}) }}">{{ participant.gn.label }}</a></li>
		<li><a href="{{ path('groupe.detail', {'groupeGn' : groupeGn.id, 'groupe': groupeGn.groupe.id}) }}">{{ groupe.nom }}</a></li>
		<li class="active">Refuser une demande d'alliance</li>
	</ol>
	
	<div class="well bs-component">
	
		{{ form_start(form, {"attr": {"action": path('groupe.refuseAlliance', {'participant': participant.id, 'groupe': groupe.id, 'alliance': alliance.id}) }}) }}
			

			<p>Vous allez refuser une demande d'alliance faite par <strong>{{ alliance.groupe.nom }}</strong>.</p>
			<p>Etes-vous certain ?</p>

			{{ form(form) }}
			
			<p>Le chef de groupe recevra une notification pour le prévenir de votre refus.</p>
			 
		{{ form_end(form) }}
	
	</div>
	
{% endblock %}
