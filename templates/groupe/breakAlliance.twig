{% extends "base.html.twig" %}

{% block title %}Briser une alliance{% endblock title %}

{% block body %}

    <ol class="breadcrumb">
        <li><a href="{{ path('homepage') }}">Page d'accueil</a></li>
        <li><a href="{{ path('participant.index', {'participant' : participant.id}) }}">{{ participant.gn.label }}</a>
        </li>
        <li><a href="{{ path('groupeGn.groupe', {'groupeGn' : groupeGn.id}) }}">{{ groupe.nom }}</a></li>
        <li class="active">Briser une alliance</li>
    </ol>

    {% set cardContent %}
        <p>Vous allez briser alliance entre <strong>{{ alliance.groupe.nom }}</strong> et
            <strong>{{ alliance.requestedGroupe.nom }}</strong>.</p>
        <p>Etes-vous certain ?</p>

        <p>Le chef de groupe recevra une notification pour le prévenir de votre choix.</p>
    {% endset %}

    {{ include("_partials/form.twig",{
        'title': 'Briser une alliance',
        'data': {'content': cardContent},
        'form' : form}) }}

{% endblock %}
