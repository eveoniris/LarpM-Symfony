{% extends "base.html.twig" %}

{% block title %}Competences par GN{% endblock title %}

{% block body %}

    {% include '_partials/breadcrumb.twig' with {'items' :
        [
            {'name' : 'Statistiques', 'route': path('stats.list') },
            {'name' : 'Répartition des constructions sur les fiefs'},
        ]
    } %}

    <div class="d-flex justify-content-end">

        <div class="btn-toolbar" role="toolbar" aria-label="...">
            <div class="btn-group" role="group" aria-label="...">
                <a class="btn btn-secondary" data-toggle="tooltip" data-placement="bottom"
                   title="Télécharger" href="{{ path('stats.constructions.fiefs.csv') }}"><i
                            class="fa fa-file-csv"></i>
                    Télécharger</a>
                <a class="btn btn-secondary" data-toggle="tooltip" data-placement="bottom"
                   title="format JSON" href="{{ path('stats.constructions.fiefs.json') }}"><i
                            class="fa fa-file-lines"></i>
                    Json API</a>
            </div>
        </div>
    </div>

    <div class="well well-sm">
        <div class="header">
            <h5>Répartition des constructions sur les fiefs</h5>
        </div>

        <table class="table table-striped table-bordered table-condensed table-hover">
            {% include '_partials/thead.twig' with  { 'route': 'stats.list', 'rows': [
                {'name' : 'Bâtiment', 'css': 'text-center'},
                {'name' : 'Fief', 'css': 'text-center'},
                {'name' : 'Description'},
            ]} %}
            <tbody>
            {% for construction in constructions %}
                <tr>
                    <td class="text-center col-1">{{ construction.batiment }}</td>
                    <td class="text-center col-1">{{ construction.fiefs }}</td>
                    <td class="col-10">{{ construction.description|raw }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

    </div>

{% endblock %}
