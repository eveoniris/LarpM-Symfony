{% extends "base.html.twig" %}

{% block title %}Type de groupe transverse{% endblock title %}

{% block body %}

    <ol class="breadcrumb">
        <li><a href="{{ path('homepage') }}">Accueil</a></li>
        <li><a href="{{ path("groupeSecondaire.list") }}">Liste des groupes transverses</a></li>
        <li class="active">Modification d'un type de groupe transverse</li>
    </ol>

    <div class="well bs-component">
        {{ form_start(form, {"attr": {"action": path('groupeSecondaireType.update',{'index': groupeSecondaireType.id}) }}) }}
        <fieldset>
            <legend>Modification d'un type de groupe transverse</legend>

            {{ form(form) }}
        </fieldset>
        {{ form_end(form) }}
    </div>

{% endblock body %}
