{% extends "base.print.twig" %}

{% block content %}

    {% for groupe in groupes %}

        {% for background in groupe.backs('GROUPE_OWNER') %}
            {% if background.gn == gn %}

                <h2>#{{ groupe.numero }} -- {{ groupe.nom }}</h2>
                <h3>Ce background est destiné au chef du groupe</h3>
                {{ background.text|markdown_to_html }}
                <hr style="page-break-after: always;"/>
            {% endif %}
        {% endfor %}

    {% endfor %}

{% endblock content %}
