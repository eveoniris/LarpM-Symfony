{% extends "base.html.twig" %}

{% block title %}Etat-civil{% endblock %}

{% block body %}

    <ol class="breadcrumb">
        <li><a href="{{ path('homepage') }}">Accueil</a></li>
        <li><a href="{{ path('user.list') }}">Liste des utilisateurs</a></li>
        <li class="active">Etat-civil d'un utilisateur</li>
    </ol>

    <div class="well well-sm">

        <div class="header">
            <h5>Etat-civil d'un utilisateur de {{ etatCivil.prenom }} {{ etatCivil.nom }}</h5>
        </div>


        <table class="table table-striped table-bordered table-condensed table-hover">

            <thead>
            <tr>
                <th class="col-md-4">Propriété</th>
                <th class="col-md-8">Valeur</th>
            </tr>
            </thead>
            <tbody>
            <tr>
                <th>Nom civil</th>
                <td>{{ etatCivil.nom }}</td>
            </tr>
            <tr>
                <th>Prénom civil</th>
                <td>{{ etatCivil.prenom }}</td>
            </tr>
            <tr>
                <th>Date de naissance</th>
                <td>
                    {% if etatCivil.dateNaissance %}
                        {{ etatCivil.dateNaissance|date('Y-m-d') }}
                    {% else %}
                        Non renseigné
                    {% endif %}
                </td>
            </tr>
            <tr>
                <th>Prénom d'usage</th>
                <td>{{ etatCivil.prenomUsage }}</td>
            </tr>
            <tr>
                <th>Numéro de téléphone</th>
                <td>{{ etatCivil.telephone }}</td>
            </tr>
            <tr>
                <th>Restriction alimentaire</th>
                <td>{% if etatCivil.user.restrictions|length > 0 %}{{ etatCivil.user.restrictions|join(', ') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Problèmes médicaux</th>
                <td>{{ etatCivil.problemeMedicaux }}</td>
            </tr>
            <tr>
                <th>Personne à prévenir en cas d'urgence</th>
                <td>{{ etatCivil.personneAPrevenir }}</td>
            </tr>
            <tr>
                <th>Numéro de téléphone de la personne à prévenir</th>
                <td>{{ etatCivil.telPap }}</td>
            </tr>
            <tr>
                <th>Numéro d'adhérant à la FédéGn</th>
                <td>{{ etatCivil.fedegn }}</td>
            </tr>
            </tbody>
        </table>
    </div>
{% endblock body %}
