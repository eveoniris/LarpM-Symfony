{% extends "base.html.twig" %}

{% block title %}{{ groupe.nom }}{% endblock title %}

{% block body %}

	<ol class="breadcrumb">
		<li><a href="{{ path('homepage') }}">Page d'accueil</a></li>
		<li><a href="{{ path('participant.index', {'participant' : participant.id}) }}">{{ participant.gn.label }}</a></li>
		<li><a href="{{ path('groupe.detail', {'groupeGn' : groupeGn.id, 'groupe': groupeGn.groupe.id}) }}">{{ groupeGn.groupe.nom }}</a></li>
		<li class="active">Places disponibles</li>
	</ol>
	
	<div class="well well-sm">
		<div class="header">
			<h5>Nombre de place disponibles</h5>
		</div>						
						
		{{ form_start(form, {"attr": {"action": path('groupeGn.placeAvailable', {'groupe': groupeGn.groupe.id, 'groupeGn': groupeGn.id}) }}) }}
			
			{{ form(form) }}
		{{ form_end(form) }}
	</div>
	
{% endblock body %}
