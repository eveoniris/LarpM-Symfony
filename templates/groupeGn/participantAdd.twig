{% extends "base.html.twig" %}

{% block title %}Groupes{% endblock title %}

{% block body %}

	<ol class="breadcrumb">
		<li><a href="{{ path('homepage') }}">Accueil</a></li>
		<li><a href="{{ path("groupe.list") }}">Liste des groupes</a></li>
		<li><a href="{{ path("groupe.detail", {'groupe': groupeGn.groupe.id}) }}">{{ groupeGn.groupe.nom }}</a></li>
		<li><a href="{{ path("groupeGn.list", {'groupe': groupeGn.groupe.id}) }}">Liste des sessions</a></li>
		<li class="active">Ajouter un joueur à une session</li>
	</ol>
	
	<div class="well well-sm">
		<blockquote>Vous allez ajouter un joueur au groupe {{ groupeGn.groupe }} pour la session {{ groupeGn.gn }}.
		Ne sont présentés dans la liste ci-dessous que les joueurs n'étant pas encore affecté à un groupe pour le jeu {{ groupeGn.gn }}</blockquote>
		{{ form_start(form, {"action": path('groupeGn.participants.add',{'groupeGn': groupeGn.id})}) }}			
			{{ form(form) }}
		{{ form_end(form) }}
	</div>
{% endblock %}
