{% extends "base.html.twig" %}

{% block title %}{{ personnage.publicName }}{% endblock title %}

{% block body %}
<div class="container-fluid">

	<ol class="breadcrumb">
		<li><a href="{{ path('homepage') }}">Accueil</a></li>
		<li><a href="{{ path('personnage.admin.list') }}">Liste des personnages</a></li>
		<li><a href="{{ path('personnage.admin.detail', {'personnage': personnage.id}) }}">Détail de {{ personnage.publicName }}</a></li>
		<li class="active">Choix des domaines de magie</li>
	</ol>
	
	<div class="well bs-component">
		{{ form_start(form, {"attr": {"action": path('personnage.admin.update.domaine',{'personnage':personnage.id}) }}) }}
			<fieldset>
				<legend>{{ personnage.publicName }} - <small>Choix des domaines de magie</small></legend>
				<p>Choisissez les domaines de magie du personnage</p>
				
	
				{{ form_row(form.domaines) }}
				{{ form_rest(form) }}
			</fieldset>
		{{ form_end(form) }}
	</div>
{% endblock body %}