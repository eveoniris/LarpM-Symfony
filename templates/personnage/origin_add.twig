{% extends "base.html.twig" %}

{% block title %}{{ personnage.publicName }}{% endblock title %}

{% block body %}
    <div class="container-fluid">

    <ol class="breadcrumb">
        <li><a href="{{ path('homepage') }}">Accueil</a></li>
        <li><a href="{{ path('gn.detail', {'gn' : participant.gn.id}) }}">{{ participant.gn.label }}</a></li>
        <li><a href="{{ path('gn.personnage', {'gn': participant.gn.id}) }}">{{ participant.personnage.nom }}</a></li>
        <li class="active">Choix d'une origine</li>
    </ol>

    <div class="well bs-component">
        <p>Choisissez l'origine de votre personnage, en général, les personnages sont issus du même pays que celui de
            leur groupe !</p>
        <p>Attention ! une fois votre origine choisie, vous ne pourrez plus revenir sur votre choix.</p>

        {{ form(form) }}

    </div>
{% endblock body %}
