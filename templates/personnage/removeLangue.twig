{% extends "base.html.twig" %}

{% block title %}Personnages{% endblock title %}

{% block body %}

	<ol class="breadcrumb">
		<li><a href="{{ path('homepage') }}">Accueil</a></li>
		<li><a href="{{ path('personnage.admin.list') }}">Liste des personnages</a></li>
		<li><a href="{{ path('personnage.admin.detail', {'personnage': personnage.id}) }}">Détail de {{ personnage.publicName }}</a></li>
		<li class="active">Retrait d'une langue</li>
	</ol>

	<div class="well bs-component">	
		{{ form_start(form, {"attr": {"action": path('personnage.admin.delete.langue', {'personnage': personnage.id,'personnageLangue': personnageLangue.id}) }}) }}
			<fieldset>
				<legend>Retrait d'une langue</legend>
				
				Attention, vous vous appretez à retirer la langue <b>{{ personnageLangue.langue }}</b> du personnage {{ personnage.publicName }}.
				{{ form(form) }}
			</fieldset>
		{{ form_end(form) }}
	</div>
	
{% endblock body %}