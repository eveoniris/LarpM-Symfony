{% extends "base.html.twig" %}

{% block title %}Cosmogonie{% endblock title %}

{% block body %}

    {% include '_partials/breadcrumb.twig' with {'items' :
        [
            {'name' : 'Liste de la cosmogonie' },
        ]
    } %}

    <div class="well well-sm">

        <div class="header">
            <h5>Liste de la cosmogonie</h5>
        </div>

        {% include '_partials/formListSearch.twig' with
            {
                'canAdd': {'title': 'Ajouter une entrée', 'path': path('cosmogonie.add')},
                'actionPath' : path('cosmogonie.list')
            } %}
    </div>

    {% include '_partials/pagination.html.twig' %}

    <div class="table-responsive">
    <table class="table table-sm table-striped table-bordered table-hover">
        {% include '_partials/thead.twig' with  { 'route': 'cosmogonie.list', 'rows': [
            {'name' : 'Id', 'field' : 'id', 'css' : 'text-center'},
            {'name' : 'Titre', 'field' : 'titre', 'css' : 'text-center'},
            {'name' : 'Description', 'field' : 'description'},
            {'name' : 'Actions', 'css' : 'text-center'},
        ]} %}
        <tbody>
        {% for cosmogonie in paginator %}
            <tr>
                <td class="text-center align-middle col-1">
                    {{ cosmogonie.id|highlight(pagerService.searchValue) }}
                </td>
                <td class="text-center align-middle col-1">{{ cosmogonie.titre|highlight(pagerService.searchValue) }}</td>
                <td>
                    {{ cosmogonie.description|markdown_to_html|highlight(pagerService.searchValue) }}
                </td>
                <td class="col-1">
                    <div class="btn-toolbar float-end" role="toolbar">
                        <div class="btn-group-vertical" role="group">
                            <a class="btn btn-secondary" data-bs-toggle="tooltip" data-bs-placement="bottom"
                               title="Modifier"
                               href="{{ path('cosmogonie.update',{'cosmogonie':cosmogonie.id}) }}"><i
                                        class="fa fa-pencil"></i> Modifier</a>
                            <a class="btn btn-secondary" data-bs-toggle="tooltip" data-bs-placement="bottom"
                               title="Détail"
                               href="{{ path('cosmogonie.detail',{'cosmogonie':cosmogonie.id}) }}"><i
                                        class="fa fa-eye"></i> Detail</a>
                            <a class="btn btn-secondary" data-bs-toggle="tooltip" data-bs-placement="bottom"
                               title="Supprimer"
                               href="{{ path('cosmogonie.delete',{'cosmogonie':cosmogonie.id}) }}"><i
                                        class="fa fa-eraser"></i> Supprimer</a>
                        </div>
                    </div>
                </td>
            </tr>
        {% else %}
            {% include '_partials/noResultTableRow.twig' %}
        {% endfor %}
        </tbody>
    </table>
    </div>

    {% include '_partials/pagination.html.twig' %}


{% endblock %}
