{% extends "base.html.twig" %}

{% block title %}Connaissances{% endblock title %}

{% block body %}

	{% include '_partials/breadcrumb.twig' with {'items' :
		[
			{'name' : 'Liste des classes', 'route': path('classe.list') },
			{'name' : classe.label, 'route': path('classe.detail', {'classe': classe.id}) },
			{'name' : 'Personnages ayant cette classe' },
		]
	} %}

	{#
	{% if personnage.vivant == 0 %}💀 {% endif %}#{{ personnage.id }} - {{ personnage.identity }}
	-- {{ personnage.classeName }}
	#}
	{{ include('admin/personnage/search.twig',
		 	{
			 displayAdminToolbar: false,
			 displaySearchFilter: false,
		  	}
  		)
	}}
	
{% endblock body %}
