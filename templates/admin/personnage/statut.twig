{% extends "base.html.twig" %}

{% block title %}{{ personnage.publicName }}{% endblock title %}

{% block body %}
<div class="container-fluid">

	<ol class="breadcrumb">
		<li><a href="{{ path('homepage') }}">Accueil</a></li>
		<li><a href="{{ path('personnage.admin.list') }}">Liste des personnages</a></li>
		<li><a href="{{ path('personnage.admin.detail', {'personnage': personnage.id}) }}">Détail de {{ personnage.publicName }}</a></li>
		<li class="active">Statut du personnage</li>
	</ol>
	
	<div class="well bs-component">
		{{ form_start(form, {"attr": {"action": path('personnage.admin.statut',{'personnage':personnage.id}) }}) }}
			<fieldset>
				<legend>{{ personnage.publicName }} - <small>Statut du personnage</small></legend>
				<p>Indiquez si le personnage est vivant ou mort</p>
				
	
				{{ form(form) }}
			</fieldset>
		{{ form_end(form) }}
	</div>
{% endblock body %}