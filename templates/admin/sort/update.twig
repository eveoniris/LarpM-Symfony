{% extends "base.html.twig" %}

{% block title %}Sorts{% endblock title %}

{% block body %}

	<ol class="breadcrumb">
		<li><a href="{{ path('homepage') }}">Accueil</a></li>
		<li><a href="{{ path('magie.sort.list') }}">Liste des sorts</a></li>
		<li class="active">Modifier un sort</li>
	</ol>
	
	
	<div class="well bs-component">
		{{ form_start(form, {"attr": {"action": path('magie.sort.update', {'sort': sort.id}) }}) }}
			<fieldset>
				<legend>Modification d'un sort</legend>
					
					{{ form(form) }}
			</fieldset>
		{{ form_end(form) }}
	</div>
				
{% endblock body %}