{% extends "base.html.twig" %}

{% block title %}Territoire{% endblock title %}

{% block body %}

<div class="well bs-component">
	
	
	<div class="header"><h5>Modifier les lois pour le territoire {{ territoire.nom }}</h5></div>
	
	
	{{ form_start(form, {"attr": {"action": path('territoire.updateLoi', {'territoire': territoire.id}) }}) }}			
			
			{{ form(form) }}
	{{ form_end(form) }}
	
</div>

{% endblock body %}