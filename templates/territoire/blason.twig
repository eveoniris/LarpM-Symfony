{% extends "base.html.twig" %}

{% block title %}Blason{% endblock title %}

{% block body %}

<div class="well bs-component">
	
	
	<div class="header"><h5>Ajouter un blason pour le territoire {{ territoire.nom }}</h5></div>
	
	<blockquote>Choisissez votre fichier et téléversez le dans larpManager.</blockquote>
	
	{{ form_start(form, {"attr": {"action": path('territoire.update.blason', {'territoire': territoire.id}) }}) }}			
			
			{{ form(form) }}
	{{ form_end(form) }}
	
</div>

{% endblock body %}